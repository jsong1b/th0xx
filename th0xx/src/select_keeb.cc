// This is a function that prompts the user to select a keyboard. Returns
// either a pointer to the keyboard or NULL if the user decides to exit the
// the program or when there is an error.

#include "../include/frontend.h"

void selectKeeb(void) {
    // TODO(shell): implement `select_keeb`
    return;
}
